<% include partials/header %>

  <table class="table table-striped table-dark">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Name</th>
        <th scope="col">Date</th>
        <th scope="col">Response</th>
      </tr>
    </thead>

    <tbody>


      <!-- users.name
      availability.option
      time_slots.response -->


          <% var i = 1 %>
          <% users.forEach((user) => { %>
            <tr>
              <th scope="row"> <%= i++ %> </th>
                <td><%= user.name %> </td>
                <td><%= user.response %></td>
                <td><a class="btn btn-warning" href="/events/<%= shortURL %>/edit">Edit</a></td>
            </tr>
          <% }); %>


    </tbody>
  </table>

<% include partials/footer %>







<!-- //last page where u see all users, their choices
show to visitor
u have rsvped to party title
at location location
for organizer X org name

+ your choice of slot


events/:id/main -->
